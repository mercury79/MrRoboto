// Plantilla de credenciales. COPIA este archivo a "secrets.h" (mismo
// directorio) y rellena tus datos. secrets.h esta en .gitignore: nunca
// sube al repo, tu contrasena WiFi se queda en tu maquina.
//
//   cp src/secrets.h.example src/secrets.h   (y editas los valores)

#pragma once

#define WIFI_SSID  "TU_RED_WIFI"
#define WIFI_PASS  "TU_PASSWORD"

// Nombre con el que el ESP32 aparece en la red y contrasena para el OTA.
// La contrasena OTA evita que cualquiera en tu LAN te reflashee la placa.
#define OTA_HOSTNAME  "mrroboto"
#define OTA_PASSWORD  "cambia-esto"
